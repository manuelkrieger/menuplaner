{% import '_macro/nav_helper.twig' as navhelper %}
<div class="be-left-sidebar">
  <div class="left-sidebar-wrapper">
    <a href="#" class="left-sidebar-toggle">CCT</a>
    <div class="left-sidebar-spacer">
      <div class="left-sidebar-scroll">
        <div class="left-sidebar-content">
          <ul class="sidebar-elements">
            <li class="divider">Menu</li>
            {{ navhelper.menuItem(path('homepage'), 'view-dashboard', 'Dashboard') }}
            {% if is_granted('ROLE_UNITY_CRUD') %}
              {{ navhelper.menuItem(path('unity_list'), 'ruler', 'Einheiten', isSameRoute('unity_list')) }}
            {% endif %}
            {% if is_granted('ROLE_FOOD_LIST') %}
              {{ navhelper.menuItem(path('food_list'), 'pizza', 'Lebensmittel', isSameRoute('food_list')) }}
            {% endif %}
            {% if is_granted('ROLE_USER_LIST') %}
              {{ navhelper.menuItem(path('users_list'), 'accounts', 'Benutzer', isSameRoute('users_list')) }}
            {% endif %}
            <li>
              <a href="http://foxythemes.net/preview/products/beagle2/" target="_blank">Template-Demo</a>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="progress-widget">
      <div class="progress-data"><span class="progress-value">60%</span><span class="name">Current Project</span>
      </div>
      <div class="progress">
        <div style="width: 60%;" class="progress-bar progress-bar-primary"></div>
      </div>
    </div>
  </div>
</div>
